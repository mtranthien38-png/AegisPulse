[tool.poetry]
name = "aegispulse-backend"
version = "0.1.0"
description = "AegisPulse backend"
authors = ["mtranthien38-png"]

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.115.0"
uvicorn = { version = "^0.30.0", extras = ["standard"] }
pydantic = "^2.8.0"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0.0"

